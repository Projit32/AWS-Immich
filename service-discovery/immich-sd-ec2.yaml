AWSTemplateFormatVersion: '2010-09-09'
Description: Immich Valkey Traefik EC2 Instance

Parameters:

  InstanceType:
    Type: String
    Default: t4g.nano

  VpcId:
    Type: AWS::EC2::VPC::Id

  SubnetId:
    Type: AWS::EC2::Subnet::Id

  BucketName:
    Type: String

  KeyPair:
    Type: String
    Default: ""

  ImageLocationS3Uri:
    Type: String

Conditions:
  HasKey: !Not [!Equals [!Ref KeyPair, ""]]

Resources:

  TraefikSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Security Group for Traefik
      VpcId: !Ref VpcId

      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 22
          ToPort: 22
          CidrIp: 0.0.0.0/0
          Description: SSH access
        - IpProtocol: tcp
          FromPort: 80
          ToPort: 80
          CidrIp: 0.0.0.0/0
          Description: Traefik EntryPoint
        - IpProtocol: tcp
          FromPort: 8080
          ToPort: 8080
          CidrIp: 0.0.0.0/0
          Description: Traefik Dashboard

      SecurityGroupEgress:
        - IpProtocol: "-1"
          CidrIp: 0.0.0.0/0

  TraefikInstanceRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "${AWS::StackName}-Traefik-role"

      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service:
                - ec2.amazonaws.com
            Action:
              - sts:AssumeRole

      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore

      Policies:
        - PolicyName: S3Access
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - s3:GetObject
                  - s3:GetObjectVersion
                  - s3:HeadObject
                Resource:
                  - !Sub arn:aws:s3:::${BucketName}/*
              - Effect: Allow
                Action:
                  - s3:ListBucket
                Resource:
                  - !Sub arn:aws:s3:::${BucketName}
        - PolicyName: TraefikECSReadAccess
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Effect: Allow
                Action:
                  - "ecs:ListClusters"
                  - "ecs:DescribeClusters"
                  - "ecs:ListTasks"
                  - "ecs:DescribeTasks"
                  - "ecs:ListContainerInstances"
                  - "ecs:DescribeContainerInstances"
                  - "ecs:DescribeTaskDefinition"
                  - "ec2:DescribeInstances"
                Resource: "*"

  TraefikInstanceProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      Roles:
        - !Ref TraefikInstanceRole

  TraefikInstance:
    Type: AWS::EC2::Instance
    Properties:

      ImageId: !Sub "{{resolve:ssm:/aws/service/ecs/optimized-ami/amazon-linux-2023/arm64/recommended/image_id}}"

      InstanceType: !Ref InstanceType

      IamInstanceProfile: !Ref TraefikInstanceProfile

      KeyName: !If
        - HasKey
        - !Ref KeyPair
        - !Ref AWS::NoValue

      MetadataOptions:
        HttpEndpoint: enabled
        HttpTokens: required

      NetworkInterfaces:
        - DeviceIndex: 0
          SubnetId: !Ref SubnetId
          GroupSet:
            - !Ref TraefikSecurityGroup

      BlockDeviceMappings:
        - DeviceName: /dev/xvda
          Ebs:
            VolumeType: gp3
            VolumeSize: 30
            DeleteOnTermination: true
            Encrypted: true

      UserData:
        Fn::Base64: !Sub |
          #!/bin/bash
          set -euxo pipefail
          exec > >(tee /var/log/traefik.log | logger -t traefik) 2>&1

          until systemctl is-active --quiet docker; do
            sleep 2
          done
        
          aws s3 cp ${ImageLocationS3Uri}/traefik.tar .
          docker load -i traefik.tar
          
          cat >/etc/systemd/system/treafik.service <<'EOF'
          [Unit]
          Description=Treafik
          Requires=docker.service
          After=docker.service

          [Service]
          Type=simple
          Restart=always
          RestartSec=5

          ExecStartPre=-/usr/bin/docker rm -f traefik

          ExecStart=/usr/bin/docker run \
            --name traefik \
            --restart unless-stopped \
            -p 80:80 \
            -p 8080:8080 \
            -e AWS_REGION=us-east-1 \
            traefik:latest \
            --api.insecure=true \
            --providers.ecs=true \
            --providers.ecs.clusters=immich-cluster \
            --providers.ecs.region=us-east-1 \
            --providers.ecs.exposedByDefault=false \
            --entrypoints.web.address=:80

          ExecStop=/usr/bin/docker stop -t 20 traefik

          ExecStopPost=-/usr/bin/docker rm -f traefik

          [Install]
          WantedBy=multi-user.target
          EOF

          systemctl daemon-reload
          systemctl enable treafik.service
          systemctl start treafik.service

Outputs:

  InstanceId:
    Value: !Ref TraefikInstance

  PrivateIp:
    Value: !GetAtt TraefikInstance.PrivateIp

  AvailabilityZone:
    Value: !GetAtt TraefikInstance.AvailabilityZone

  SecurityGroupId:
    Value: !Ref TraefikSecurityGroup

  InstanceProfile:
    Value: !Ref TraefikInstanceProfile

  Role:
    Value: !Ref TraefikInstanceRole